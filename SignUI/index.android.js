//this approach synchronizes iOS and Android index
//since they use identical components ok
import { AppRegistry } from 'react-native';
import App from './src/App';

AppRegistry.registerComponent('SignUI', () => App);
